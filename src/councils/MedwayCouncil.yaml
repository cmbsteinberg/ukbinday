council: MedwayCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://api.medway.gov.uk/api/waste/getwasteday/{user_uprn}
api_methods:
- GET
api_description: Fetches bin collection data directly from Medway's API using the
  UPRN.
response_format: json
date_format: '%Y-%m-%dT%H:%M:%S%z'
test_inputs:
  uprn: '200000907059'
