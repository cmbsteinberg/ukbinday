council: EastCambridgeshireCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://eastcambs-self.achieveservice.com/appshost/firmstep/self/apps/custompage/bincollections
api_methods:
- GET
api_description: Takes a UPRN as a query parameter and returns HTML containing bin
  collection rows.
response_format: html
bin_selector: div.row.collectionsrow
date_format: '%a - %d %b %Y'
test_inputs:
  uprn: '10002597178'
