council: EastDunbartonshireCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.eastdunbarton.gov.uk/services/a-z-of-services/bins-waste-and-recycling/bins-and-recycling/collections/
api_methods:
- GET
api_description: The council website provides a direct URL that displays bin collection
  dates in an HTML table when the UPRN is passed as a query string parameter.
response_format: html
bin_selector: table.bin-table tbody tr
date_format: '%A, %d %B %Y'
test_inputs:
  uprn: '132027197'
