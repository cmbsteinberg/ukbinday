council: EastHertsCouncil
request_type: token_then_api
required_user_input:
- uprn
api_urls:
- https://eastherts-self.achieveservice.com/authapi/isauthenticated
- https://eastherts-self.achieveservice.com/apibroker/runLookup
api_methods:
- GET
- POST
api_description: Initial GET call to the authapi endpoint retrieves an 'auth-session'
  SID. This SID is then used as a query parameter in a POST request to the apibroker
  runLookup endpoint, passing the UPRN in the JSON body under 'inputUPRN'.
response_format: json
date_format: '%A %d %B'
test_inputs:
  uprn: '10023088183'
