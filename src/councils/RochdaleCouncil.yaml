council: RochdaleCouncil
request_type: token_then_api
required_user_input:
- postcode
- uprn
api_urls:
- https://rochdale-self.achieveservice.com/authapi/isauthenticated
- https://rochdale-self.achieveservice.com/apibroker/runLookup
api_methods:
- GET
- POST
api_description: The process involves getting an authentication session ID, then making
  a lookup request to retrieve a Bartec token, and finally a POST request with the
  UPRN and Bartec token to retrieve bin collection dates.
response_format: json
date_format: '%Y-%m-%dT%H:%M:%S'
test_inputs:
  uprn: '10094358428'
  postcode: OL11 5BE
