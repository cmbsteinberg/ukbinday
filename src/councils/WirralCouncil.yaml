council: WirralCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://ww3.wirral.gov.uk//recycling/detailContentDru7.asp
api_methods:
- GET
api_description: 'The council website uses a direct URL with query parameters for
  the street, suburb, and waste type. It iterates through three specific waste types:
  Non recycleable (green bin), Garden waste (brown bin), and Paper and packaging (grey
  bin).'
response_format: html
bin_selector: li
date_format: '%d %B %Y'
test_inputs:
  uprn: Vernon Avenue,Seacombe
