council: HighlandCouncil
request_type: token_then_api
required_user_input:
- uprn
api_urls:
- https://highland-self.achieveservice.com/authapi/isauthenticated?uri=https%3A%2F%2Fhighland-self.achieveservice.com%2Fen%2Fservice%2FCheck_your_household_bin_collection_days&hostname=highland-self.achieveservice.com&withCredentials=true
- https://highland-self.achieveservice.com/apibroker/runLookup
api_methods:
- GET
- POST
api_description: First, a GET request is made to the session URL to obtain an 'auth-session'
  ID. This session ID is then passed as a parameter 'sid' in a POST request to the
  API broker URL. The POST request includes a JSON body containing the property UPRN.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '130072429'
