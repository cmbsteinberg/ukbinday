council: FalkirkCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://recycling.falkirk.gov.uk/api/collections/{uprn}
api_methods:
- GET
api_description: A single GET request to the council API using the UPRN returns a
  JSON object containing bin types and an array of collection dates.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '136065818'
