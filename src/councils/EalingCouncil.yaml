council: EalingCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.ealing.gov.uk/site/custom_scripts/WasteCollectionWS/home/FindCollection
api_methods:
- POST
api_description: Sends a POST request with the UPRN in form data to retrieve a JSON
  response containing collection dates and service types.
response_format: json
date_format: '%d/%m/%Y'
test_inputs:
  uprn: '12073883'
