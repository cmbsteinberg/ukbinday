council: CotswoldDistrictCouncil
request_type: selenium
required_user_input:
- house_number
- postcode
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: The site uses Salesforce Experience Cloud (Community Cloud) which
  relies on complex, stateful 'aura' or 'lwc' framework calls involving context tokens,
  action descriptors, and security nonces.
test_inputs:
  postcode: GL56 0GB
