council: NorthNorfolkDistrictCouncil
request_type: selenium
required_user_input:
- postcode
- house_number
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: The system uses an OFML form (Open Form Markup Language) which usually
  involves complex state management, session-based tokens, and dynamic viewstate-like
  parameters managed by the outreach server.
test_inputs:
  postcode: NR25 6BH
