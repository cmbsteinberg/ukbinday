council: SouthCambridgeshireCouncil
request_type: id_lookup_then_api
required_user_input:
- house_number
- postcode
- skip_get_url
api_urls:
- https://servicelayer3c.azure-api.net/wastecalendar/address/search/
- https://servicelayer3c.azure-api.net/wastecalendar/collection/search/{id}/
api_methods:
- GET
- GET
api_description: First call searches for addresses based on postcode to retrieve a
  unique address ID. The second call uses that ID to fetch the collection schedule.
response_format: json
date_format: '%Y-%m-%dT%H:%M:%SZ'
test_inputs:
  postcode: CB23 6GZ
