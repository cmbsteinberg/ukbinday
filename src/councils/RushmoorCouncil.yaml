council: RushmoorCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.rushmoor.gov.uk/Umbraco/Api/BinLookUpWorkAround/Get?
api_methods:
- GET
api_description: Fetches bin collection data by passing a UPRN as the 'selectedAddress'
  parameter. The response contains an XML string wrapping a JSON object which includes
  'NextCollection' dates for different bin types.
response_format: html
bin_selector: p
date_format: '%Y-%m-%dT%H:%M:%S'
test_inputs:
  uprn: '100060545034'
