council: SouthwarkCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://services.southwark.gov.uk/bins/lookup/
api_methods:
- GET
api_description: The council provides an endpoint where the UPRN is appended to the
  base URL to return an HTML page containing bin collection schedules.
response_format: html
bin_selector: div[aria-labelledby]
date_format: '%a, %d %B %Y'
test_inputs:
  uprn: '200003469271'
