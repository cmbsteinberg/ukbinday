council: TorridgeDistrictCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://collections-torridge.azurewebsites.net/WebService2.asmx
api_methods:
- POST
api_description: A SOAP XML request is sent to the web service containing the UPRN
  and a hardcoded password 'wax01653'. The response contains an HTML-formatted string
  inside an XML wrapper which is then parsed for bin types and dates.
response_format: xml
bin_selector: b
date_format: '%a %d %b %Y'
test_inputs:
  uprn: '10091078762'
