council: LondonBoroughOfRichmondUponThames
request_type: selenium
required_user_input:
- paon
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: The page uses a form submission (likely ASP.NET or similar based
  on the structure) to filter collection data based on a street name or address provided
  by the user.
