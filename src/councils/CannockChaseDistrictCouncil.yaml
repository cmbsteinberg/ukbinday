council: CannockChaseDistrictCouncil
request_type: single_api
required_user_input:
- postcode
- uprn
api_urls:
- https://ccdc.opendata.onl/DynamicCall.dll
api_methods:
- POST
api_description: A POST request is made to a dynamic DLL endpoint with the UPRN and
  Postcode passed as form-urlencoded data. The response is an XML document containing
  collection dates.
response_format: xml
bin_selector: Collection
date_format: '%d/%m/%Y %H:%M:%S'
test_inputs:
  uprn: '200003095389'
  postcode: WS15 1JA
