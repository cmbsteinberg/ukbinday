council: CambridgeCityCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://servicelayer3c.azure-api.net/wastecalendar/collection/search/{uprn}/?authority=CCC/?numberOfCollections=255
api_methods:
- GET
api_description: Fetches bin collection data directly using the UPRN in a single API
  call to the Azure API Management service layer.
response_format: json
bin_selector: collections
date_format: '%Y-%m-%dT%H:%M:%SZ'
test_inputs:
  uprn: '200004159750'
