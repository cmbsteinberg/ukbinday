council: NorthEastLincs
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.nelincs.gov.uk/bins-and-recycling/bin-collection-schedule-display/?uprn={uprn}
api_methods:
- GET
api_description: The council provides a direct URL that takes a UPRN as a query parameter
  and returns an HTML page containing the bin collection schedule.
response_format: html
bin_selector: li.list-group-item
date_format: dd/mm/yyyy
test_inputs:
  uprn: '11062649'
