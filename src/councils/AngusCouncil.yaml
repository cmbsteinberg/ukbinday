council: AngusCouncil
request_type: selenium
required_user_input:
- postcode
- uprn
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: The service uses an iframe pointing to a Firmstep (AchieveForms)
  platform. These platforms typically use dynamic session tokens, CSRF protection,
  and complex state management across multiple steps (postcode search -> address selection
  -> results page).
test_inputs:
  uprn: '117053733'
  postcode: DD7 7LE
