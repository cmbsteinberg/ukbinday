council: RunnymedeBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.runnymede.gov.uk/homepage/150/check-your-bin-collection-day?address={uprn}
api_methods:
- GET
api_description: Calls a specific URL with the UPRN as a query parameter to retrieve
  a page containing a bin collection table.
response_format: html
bin_selector: div.widget-bin-collection table tbody tr
date_format: '%A, %d %B %Y'
test_inputs:
  uprn: '100061483636'
