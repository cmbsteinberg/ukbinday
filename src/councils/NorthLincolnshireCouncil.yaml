council: NorthLincolnshireCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://m.northlincs.gov.uk/bin_collections?no_collections=20&uprn={uprn}
api_methods:
- GET
api_description: A direct GET request to the bin collections endpoint using the UPRN
  and a limit of 20 collections. The response is expected in JSON format, potentially
  with a UTF-8-BOM.
response_format: json
date_format: '%A %d %B'
test_inputs:
  uprn: '100050194170'
