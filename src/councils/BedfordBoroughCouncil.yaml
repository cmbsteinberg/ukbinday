council: BedfordBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://bbaz-as-prod-bartecapi.azurewebsites.net/api/bincollections/residential/getbyuprn/{uprn}/35
api_methods:
- GET
api_description: A GET request to the Bartec API using the UPRN returns bin collection
  schedules in JSON format.
response_format: json
bin_selector: BinCollections
date_format: '%Y-%m-%dT%H:%M:%S'
test_inputs:
  uprn: '10024232065'
