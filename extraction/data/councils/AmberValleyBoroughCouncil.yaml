council: AmberValleyBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://info.ambervalley.gov.uk/WebServices/AVBCFeeds/WasteCollectionJSON.asmx/GetCollectionDetailsByUPRN?uprn={uprn}
api_methods:
- GET
api_description: Takes a UPRN and returns a JSON object containing keys like refuseNextDate,
  recyclingNextDate, greenNextDate, communalRefNextDate, and communalRycNextDate in
  ISO 8601 format.
response_format: json
date_format: '%Y-%m-%dT%H:%M:%S'
test_inputs:
  uprn: '100030026621'
