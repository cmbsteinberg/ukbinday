council: KingsLynnandWestNorfolkBC
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.west-norfolk.gov.uk/info/20174/bins_and_recycling_collection_dates
api_methods:
- GET
api_description: The script sends a GET request to the council bin page, but critically
  it must include the UPRN (padded to 12 digits) in a cookie named 'bcklwn_uprn' to
  retrieve the specific results.
response_format: html
bin_selector: div.bin_date_container
date_format: '%A %d %B %Y'
test_inputs:
  uprn: '10023636886'
