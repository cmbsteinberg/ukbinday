council: ForestOfDeanDistrictCouncil
request_type: selenium
required_user_input:
- house_number
- postcode
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: The automation interacts with a Salesforce Experience Cloud site.
  Data is typically fetched via the '/aura' endpoint using POST requests with serialized
  state information. Replicating these requests manually requires managing complex
  context headers and session states that are inherently tied to the browser session.
  Therefore, sticking with browser-based automation is the most reliable approach.
test_inputs:
  postcode: GL15 6JT
