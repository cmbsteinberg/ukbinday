council: RotherhamCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://bins.azurewebsites.net/api/getcollections
api_methods:
- GET
api_description: Calls the bins.azurewebsites.net API with a premisesid (mapped from
  UPRN) and a localauthority parameter set to 'Rotherham'.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '100050866000'
