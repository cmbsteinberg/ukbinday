council: LewesDistrictCouncil
request_type: single_api
required_user_input:
- uprn
- skip_get_url
api_urls:
- https://environmentfirst.co.uk/house.php?uprn={uprn}
api_methods:
- GET
api_description: Fetches bin collection data by passing the UPRN directly into the
  URL query string.
response_format: html
bin_selector: div.collect p
date_format: '%d %B %Y'
test_inputs:
  uprn: '100061930155'
