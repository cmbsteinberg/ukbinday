council: HarboroughDistrictCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://harborough.fccenvironment.co.uk/detail-address
api_methods:
- POST
api_description: Sends a JSON payload containing the UPRN to the endpoint and receives
  HTML fragment in response.
response_format: html
bin_selector: div.blocks.block-your-next-scheduled-bin-collection-days li
date_format: '%d %B %Y'
test_inputs:
  uprn: '100030489072'
