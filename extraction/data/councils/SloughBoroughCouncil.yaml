council: SloughBoroughCouncil
request_type: id_lookup_then_api
required_user_input:
- user_address
api_urls: null
api_methods: null
api_description: The website uses a keyword-based search that results in a list of
  street links. Each link leads to a specific page that appears to be dynamically
  generated or part of a directory structure where collection dates are stored in
  list items.
response_format: html
bin_selector: dt.definition__heading
date_format: '%d %B %Y'
test_inputs:
  postcode: SL2 2EW
