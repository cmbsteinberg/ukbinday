council: HertsmereBoroughCouncil
request_type: single_api
required_user_input:
- postcode
- house_number
api_urls:
- https://hertsmere-services.onmats.com/w/webpage/round-search
api_methods:
- POST
api_description: '[AUTO-CORRECTED from id_lookup_then_api] The site uses an Onmats/WDM
  platform that typically requires a session token and multi-step form submissions
  to identify an address and then retrieve the schedule.'
api_headers:
  Content-Type: application/x-www-form-urlencoded
api_payload_example: _target_step=address_selection&postcode=WD7 9HZ&address_id=12345678
response_format: html
date_format: dd/MM/yyyy
test_inputs:
  postcode: WD7 9HZ
