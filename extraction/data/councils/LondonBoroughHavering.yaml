council: LondonBoroughHavering
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://lbhapiprod.azure-api.net/whitespace/GetCollectionByUprnAndDate
api_methods:
- POST
api_description: Uses a single POST request to an Azure API Management endpoint. It
  requires a fixed subscription key in the headers and a JSON body containing the
  UPRN and the current date.
response_format: json
date_format: '%d/%m/%Y %H:%M:%S'
test_inputs:
  uprn: '100021380730'
