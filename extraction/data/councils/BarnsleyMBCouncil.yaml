council: BarnsleyMBCouncil
request_type: single_api
required_user_input:
- postcode
- uprn
api_urls:
- https://waste.barnsley.gov.uk/ViewCollection/SelectAddress
api_methods:
- POST
api_description: '[AUTO-CORRECTED from id_lookup_then_api] Submits a POST request
  containing the user''s postcode and UPRN to retrieve a collection schedule HTML
  page.'
response_format: html
bin_selector: fieldset
date_format: '%A, %B %d, %Y'
test_inputs:
  uprn: '2007004502'
  postcode: S36 9AN
