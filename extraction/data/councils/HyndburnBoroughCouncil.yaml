council: HyndburnBoroughCouncil
request_type: id_lookup_then_api
required_user_input:
- postcode
- uprn
api_urls:
- https://itouchvision.app/portal/itouchvision/kmbd/address
- https://itouchvision.app/portal/itouchvision/kmbd/collectionDay
api_methods:
- GET
- GET
api_description: The system uses an external portal (iTouchVision) where it first
  fetches addresses for a postcode and then fetches collection days for a specific
  address selection.
api_headers:
  content-type: application/json; charset=UTF-8
response_format: html
bin_selector: div.ant-col
date_format: '%A %d %B'
test_inputs:
  uprn: '100010448773'
  postcode: BB1 4DJ
