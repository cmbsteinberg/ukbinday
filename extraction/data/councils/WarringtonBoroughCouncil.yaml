council: WarringtonBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.warrington.gov.uk/bin-collections/get-jobs/{uprn}
api_methods:
- GET
api_description: A single GET request to the council's bin collection endpoint using
  the UPRN in the URL path.
response_format: json
date_format: '%Y-%m-%dT%H:%M:%S'
test_inputs:
  uprn: '10094964379'
