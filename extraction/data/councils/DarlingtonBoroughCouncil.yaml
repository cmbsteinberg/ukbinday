council: DarlingtonBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.darlington.gov.uk/bins-waste-and-recycling/collection-day-lookup/?uprn={uprn}
api_methods:
- GET
api_description: A single GET request to the lookup URL with the UPRN as a query parameter.
  The endpoint returns HTML content intended for an AJAX request.
response_format: html
bin_selector: '#detailsDisplay .refuse-results'
date_format: '%A %d %B %Y'
test_inputs:
  uprn: '10003076924'
