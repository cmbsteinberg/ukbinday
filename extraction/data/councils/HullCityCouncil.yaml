council: HullCityCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.hull.gov.uk/ajax/bin-collection?bindate={uprn}
api_methods:
- GET
api_description: A single GET request using the UPRN as a query parameter called 'bindate'
  which returns a JSON array of bin collection schedules.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '21033995'
