council: HinckleyandBosworthBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.hinckley-bosworth.gov.uk/set-location?id={uprn}&redirect=refuse&rememberloc=
api_methods:
- GET
api_description: The UPRN is used to set the location cookie which redirects to the
  refuse page containing the collection schedule.
response_format: html
bin_selector: div.first_date_bins, div.last_date_bins
date_format: '%d %B'
test_inputs:
  uprn: '100030533512'
