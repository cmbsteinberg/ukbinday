council: TonbridgeAndMallingBC
request_type: token_then_api
required_user_input:
- postcode
- uprn
api_urls:
- https://www.tmbc.gov.uk/xfp/form/167
api_methods:
- POST
api_description: Submits a multipart/form-data request containing the user's postcode
  and UPRN to a specific form endpoint. It uses a token and page ID to retrieve a
  table of collection dates.
response_format: html
bin_selector: table.data-table.waste-collections-table tbody tr
date_format: '%a %d %B'
test_inputs:
  uprn: '10002914589'
  postcode: ME19 4JS
