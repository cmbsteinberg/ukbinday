council: ConwyCountyBorough
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.conwy.gov.uk/Contensis-Forms/erf/collection-result-soap-xmas2025.asp?ilangid=1&uprn={uprn}
api_methods:
- GET
api_description: A single GET request to a URL containing the UPRN returns an HTML
  page containing bin collection schedules.
response_format: html
bin_selector: div[class*="containererf"]
date_format: '%A, %d/%m/%Y'
test_inputs:
  uprn: '100100429249'
