council: GuildfordCouncil
request_type: selenium
required_user_input:
- postcode
- house_number
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: Salesforce sites typically require maintaining state across multiple
  XHR requests (Aura/LWC components). While technically possible to reverse engineer,
  it is extremely fragile compared to browser automation due to the way Salesforce
  handles CSRF tokens and component actions. Direct requests are not recommended for
  this platform.
test_inputs:
  postcode: GU3 1AH
