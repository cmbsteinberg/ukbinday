council: CherwellDistrictCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.cherwell.gov.uk/homepage/129/bin-collection-search?uprn={uprn}
api_methods:
- GET
api_description: Calls the bin collection search page with the UPRN in the query string
  and parses the resulting HTML.
response_format: html
bin_selector: div.bin-collection-results__tasks li.list__item
date_format: '%d %B %Y'
test_inputs:
  uprn: '100121292407'
