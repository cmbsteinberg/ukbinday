council: ArmaghBanbridgeCraigavonCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.armaghbanbridgecraigavon.gov.uk/resident/binday-result/?address={uprn}
api_methods:
- GET
api_description: Fetches a result page using the UPRN in the URL parameter 'address'.
  The resulting HTML is then parsed for specific sections matching bin types.
response_format: html
bin_selector: .heading
date_format: DD/MM/YYYY
test_inputs:
  uprn: '185625284'
