council: LondonBoroughHarrow
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.harrow.gov.uk/ajax/bins?u={uprn}&r=12345
api_methods:
- GET
api_description: Takes a UPRN and returns a JSON response containing upcoming bin
  collections including bin type and eventTime.
response_format: json
bin_selector: results/collections/all
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '100021298754'
