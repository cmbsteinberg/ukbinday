council: HastingsBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://el.hastings.gov.uk/MyArea/CollectionDays.asmx/LookupCollectionDaysByService
api_methods:
- POST
api_description: A POST request to the .asmx endpoint with a JSON payload containing
  the UPRN. The response returns an object with a property 'd' containing service
  types and lists of Unix timestamps for collection dates.
response_format: json
date_format: Unix timestamp (milliseconds)
test_inputs:
  uprn: '100060038877'
