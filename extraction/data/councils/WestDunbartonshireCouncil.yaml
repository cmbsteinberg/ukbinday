council: WestDunbartonshireCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.west-dunbarton.gov.uk/recycling-and-waste/bin-collection-day/
api_methods:
- GET
api_description: The script calls a specific URL with the UPRN passed as a query string
  parameter. It then parses the HTML response for bin types and dates.
response_format: html
bin_selector: div.round-info
date_format: '%d %B %Y'
test_inputs:
  uprn: '129001383'
