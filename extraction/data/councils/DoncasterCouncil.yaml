council: DoncasterCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.doncaster.gov.uk/Compass/PremiseDetail/GetCollectionsForCalendar
api_methods:
- GET
api_description: Uses a GET request to a JSON endpoint with UPRN and Unix epoch timestamps
  (start/end) as parameters.
response_format: json
date_format: '%Y-%m-%d %H:%M:%S'
test_inputs:
  uprn: '100050768956'
