council: TelfordAndWrekinCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://dac.telford.gov.uk/BinDayFinder/Find/PropertySearch
api_methods:
- GET
api_description: The council provides a simple GET endpoint that takes a UPRN as a
  query parameter. The response is a stringified JSON wrapped in quotes with escaped
  backslashes, which contains a list of bin collections.
response_format: json
bin_selector: bincollections
date_format: '%A %d %B'
test_inputs:
  uprn: '000452015013'
