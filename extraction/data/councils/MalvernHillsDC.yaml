council: MalvernHillsDC
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://swict.malvernhills.gov.uk/mhdcroundlookup/HandleSearchScreen
api_methods:
- POST
api_description: Takes a UPRN in a POST request as 'alAddrsel' and returns an HTML
  response containing a table of upcoming collection dates.
response_format: html
bin_selector: table tbody tr
date_format: '%A %d/%m/%Y'
test_inputs:
  uprn: '100121348457'
