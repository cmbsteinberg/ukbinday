council: SeftonCouncil
request_type: single_api
required_user_input:
- house_number
- postcode
api_urls:
- https://www.sefton.gov.uk/bins-and-recycling/bins-and-recycling/when-is-my-bin-collection-day/
api_methods:
- POST
api_description: '[AUTO-CORRECTED from id_lookup_then_api] The site uses a three-step
  POST process: first submitting the postcode and street name (or house number) to
  generate a hidden state and a list of addresses, second selecting the specific address
  from a dropdown, and finally rendering the results table.'
response_format: html
bin_selector: table
date_format: '%d/%m/%Y'
test_inputs:
  postcode: L20 6GG
