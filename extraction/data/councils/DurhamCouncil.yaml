council: DurhamCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.durham.gov.uk/bincollections?uprn=
api_methods:
- GET
api_description: Takes the UPRN and appends it to the base URL to fetch the HTML response
  containing bin collection dates.
response_format: html
bin_selector: .binsrubbish, .binsrecycling, .binsgardenwaste
date_format: '%d %B %Y'
test_inputs:
  uprn: '200003218818'
