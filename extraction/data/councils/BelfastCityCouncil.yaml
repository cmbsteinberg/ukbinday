council: BelfastCityCouncil
request_type: id_lookup_then_api
required_user_input:
- postcode
- uprn
api_urls:
- https://online.belfastcity.gov.uk/find-bin-collection-day/Default.aspx
api_methods:
- POST
api_description: The site is an ASP.NET WebForms application. It requires an initial
  GET to fetch session state variables (__VIEWSTATE, __EVENTVALIDATION), followed
  by a POST to submit the postcode and find addresses, and a final POST with the selected
  UPRN to retrieve the results table.
response_format: html
bin_selector: div#binsGrid tr
date_format: '%a%b%d%Y'
test_inputs:
  uprn: '185086469'
  postcode: BT10 0GY
