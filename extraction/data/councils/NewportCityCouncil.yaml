council: NewportCityCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://iweb.itouchvision.com/portal/itouchvision/kmbd/collectionDay
api_methods:
- POST
api_description: The API accepts an AES-CBC encrypted JSON payload containing the
  UPRN, Client ID, and Council ID. The response is a hex-encoded AES-CBC encrypted
  string which, when decrypted, contains a list of bin types and their collection
  dates.
response_format: json
date_format: MM/DD/YYYY
test_inputs:
  uprn: '100100688819'
