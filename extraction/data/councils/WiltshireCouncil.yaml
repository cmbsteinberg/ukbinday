council: WiltshireCouncil
request_type: single_api
required_user_input:
- postcode
- skip_get_url
- uprn
api_urls:
- https://ilambassadorformsprod.azurewebsites.net/wastecollectiondays/wastecollectioncalendar
api_methods:
- POST
api_description: '[AUTO-CORRECTED from id_lookup_then_api] The council provides a
  monthly calendar view. The scraper loops through the current and subsequent two
  months, sending a POST request with the UPRN and postcode to retrieve HTML calendar
  fragments for each month.'
response_format: html
bin_selector: div.cal-inner
date_format: '%Y-%m-%dT%H:%M:%S'
test_inputs:
  uprn: '100120982570'
  postcode: SN8 3TE
