council: SouthAyrshireCouncil
request_type: single_api
required_user_input:
- postcode
- uprn
api_urls:
- http://www.sac-bins.co.uk/get_calendar.php
api_methods:
- GET
api_description: The council provides an API for their Android/iOS app. It requires
  the UPRN and postcode as parameters. It returns a JSON array of collection events
  with 'className' representing the bin color and 'start' representing the date.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '141003134'
  postcode: KA19 7BN
