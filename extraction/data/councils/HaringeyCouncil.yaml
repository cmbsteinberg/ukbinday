council: HaringeyCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://wastecollections.haringey.gov.uk/property/{uprn}
api_methods:
- POST
api_description: The site accepts a POST request to the property URL containing the
  UPRN to retrieve a page with bin collection dates.
response_format: html
bin_selector: div.property-service-wrapper
date_format: dd/mm/yyyy
test_inputs:
  uprn: '100021203052'
