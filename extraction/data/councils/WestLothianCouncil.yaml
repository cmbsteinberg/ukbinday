council: WestLothianCouncil
request_type: selenium
required_user_input:
- postcode
- house_number
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: The Playwright script interacts with a postcode lookup and then selects
  an address from a dropdown before the '.bin-collections' element appears. While
  this usually maps to an 'id_lookup_then_api' pattern, Goss-based council portals
  often use server-side state tracking and AJAX callbacks that are extremely brittle
  to replicate with Python requests. Without specific network logs showing the API
  endpoints or form POST targets, it is safer to continue using Playwright.
test_inputs:
  postcode: EH52 5JE
