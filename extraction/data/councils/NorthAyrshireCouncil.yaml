council: NorthAyrshireCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.maps.north-ayrshire.gov.uk/arcgis/rest/services/AGOL/YourLocationLive/MapServer/8/query
api_methods:
- GET
api_description: Uses the ArcGIS REST API to query bin collection dates using the
  UPRN. It returns the next collection dates for Blue, Grey, Purple, and Brown bins.
response_format: json
bin_selector: features[0].attributes
test_inputs:
  uprn: '126045552'
