council: TewkesburyBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://api-2.tewkesbury.gov.uk/incab/rounds/{uprn}/next-collection
api_methods:
- GET
api_description: Direct GET request to the council's internal API using the UPRN in
  the URL path to retrieve collection schedule in JSON format.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '10067626314'
