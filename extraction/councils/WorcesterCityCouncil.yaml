council: WorcesterCityCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://selfserve.worcester.gov.uk/wccroundlookup/HandleSearchScreen
api_methods:
- POST
api_description: Sends a POST request with the UPRN in the 'alAddrsel' field to retrieve
  bin collection dates from an HTML table response.
response_format: html
bin_selector: table.table tbody tr
date_format: '%A %d/%m/%Y'
test_inputs:
  uprn: '100120650345'
