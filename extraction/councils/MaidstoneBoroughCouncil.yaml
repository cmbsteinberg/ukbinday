council: MaidstoneBoroughCouncil
request_type: selenium
required_user_input:
- postcode
- house_number
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: The site uses a complex iframe-based form system (Firmstep/Granicus)
  which typically involves multiple stateful POST requests, dynamic session tokens,
  and XHR polling to generate and display collection data within the DOM.
test_inputs:
  postcode: ME16 8BT
