council: TendringDistrictCouncil
request_type: token_then_api
required_user_input:
- postcode
- uprn
api_urls:
- https://tendring-self.achieveservice.com/en/service/Rubbish_and_recycling_collection_days
api_methods:
- POST
api_description: The site uses the Granicus (AchieveService) platform which typically
  involves complex session tokens, dynamic form IDs, and iframe-embedded state management
  that is difficult to replicate with simple requests.
api_headers:
  Content-Type: application/x-www-form-urlencoded
  X-Requested-With: XMLHttpRequest
response_format: html
bin_selector: table.wasteTable
date_format: '%d/%m/%Y'
test_inputs:
  uprn: '100090604247'
  postcode: CO15 4EU
