council: StAlbansCityAndDistrictCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://gis.stalbans.gov.uk/NoticeBoard9/VeoliaProxy.NoticeBoard.asmx/GetServicesByUprnAndNoticeBoard
api_methods:
- POST
api_description: Takes a UPRN and a noticeBoard parameter in a JSON POST body to return
  collection dates from a Veolia proxy endpoint.
response_format: json
date_format: ISO8601
test_inputs:
  uprn: '100081153583'
