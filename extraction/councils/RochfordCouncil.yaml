council: RochfordCouncil
request_type: single_api
required_user_input: []
api_urls:
- https://www.rochford.gov.uk/online-bin-collections-calendar
api_methods:
- GET
api_description: The bin collection calendar is available directly on the main page
  as a general document, not requiring specific user identification.
api_headers:
  Accept: text/html
response_format: html
bin_selector: table.responsive-enabled.govuk-table
date_format: '%A %d %B'
