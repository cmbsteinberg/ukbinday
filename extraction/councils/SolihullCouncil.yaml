council: SolihullCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://solihull.storage.googleapis.com/bins/{uprn}.html
api_methods:
- GET
api_description: A direct GET request to a Google Storage bucket URL containing an
  HTML snippet of the bin schedule for the specific UPRN.
response_format: html
bin_selector: div.mb-4.card
date_format: '%A %d %B %Y'
test_inputs:
  uprn: '100071005444'
