council: LondonBoroughLambeth
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://wasteservice.lambeth.gov.uk/WhitespaceComms/GetServicesByUprn
api_methods:
- POST
api_description: A POST request to the Lambeth waste service API using a JSON body
  containing the UPRN.
response_format: json
date_format: '%d/%m/%Y'
test_inputs:
  uprn: '100021881738'
