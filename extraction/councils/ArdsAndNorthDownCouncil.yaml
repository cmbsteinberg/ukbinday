council: ArdsAndNorthDownCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://collections-ardsandnorthdown.azurewebsites.net/WSCollExternal.asmx
api_methods:
- POST
api_description: A SOAP XML request is sent to the webaspx service with the UPRN.
  The council code is hardcoded to 'ARD'. The service returns a SOAP response containing
  escaped HTML with the collection schedule.
response_format: xml
bin_selector: b
date_format: '%a %d %b'
test_inputs:
  uprn: '187136177'
