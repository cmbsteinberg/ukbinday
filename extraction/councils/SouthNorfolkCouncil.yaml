council: SouthNorfolkCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://collections-southnorfolk.azurewebsites.net/WSCollExternal.asmx
api_methods:
- POST
api_description: A SOAP request that returns an XML envelope containing an HTML string
  within the 'getRoundCalendarForUPRNResult' tag. This HTML contains the bin collection
  dates.
response_format: xml
bin_selector: b
date_format: '%d/%m/%Y'
test_inputs:
  uprn: '2630102526'
