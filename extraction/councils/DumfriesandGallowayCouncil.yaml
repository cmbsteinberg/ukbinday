council: DumfriesandGallowayCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.dumfriesandgalloway.gov.uk/bins-recycling/waste-collection-schedule/download/{uprn}
api_methods:
- GET
api_description: The council provides an ICS (iCalendar) file for each UPRN containing
  the bin collection schedule. This file is parsed to extract event summaries and
  start dates.
response_format: xml
date_format: '%d/%m/%Y'
test_inputs:
  uprn: '137034556'
