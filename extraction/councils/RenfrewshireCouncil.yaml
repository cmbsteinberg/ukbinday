council: RenfrewshireCouncil
request_type: selenium
required_user_input:
- postcode
- paon
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: While many council forms use an underlying API, this specific script
  uses a multi-step DOM interaction where state is likely managed through session
  cookies and hidden form fields (ViewState or similar). Without network logs to confirm
  a clean API endpoint for the second step, it is safer to stick with browser automation
  as the form uses internal dynamic IDs.
test_inputs:
  postcode: PA6 7FJ
