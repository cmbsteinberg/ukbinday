council: HerefordshireCouncil
request_type: single_api
required_user_input:
- uprn
- skip_get_url
api_urls:
- https://www.herefordshire.gov.uk/rubbish-recycling/check-bin-collection-day?blpu_uprn={uprn}
api_methods:
- GET
api_description: Takes the UPRN and calls the council's bin collection page directly,
  parsing the resulting HTML.
response_format: html
bin_selector: h3.c-supplement__heading
date_format: '%A %d %B %Y'
test_inputs:
  uprn: '200002618844'
