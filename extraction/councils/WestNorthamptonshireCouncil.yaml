council: WestNorthamptonshireCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://api.westnorthants.digital/openapi/v1/unified-waste-collections/{uprn}
api_methods:
- GET
api_description: Direct API call using the UPRN to retrieve a JSON response containing
  collection items with types and dates.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '28056796'
