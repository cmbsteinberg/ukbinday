council: EastLindseyDistrictCouncil
request_type: selenium
required_user_input:
- postcode
- house_number
status: not_implemented
notes: Requires Playwright/Selenium automation - not yet implemented as HTTP requests
api_description: While technically possible to reverse-engineer Goss forms using requests
  by maintaining sessions and extracting hidden form state tokens at each step (postcode
  lookup -> address selection -> next), it is notoriously fragile and complex compared
  to Playwright. Without specific API endpoints visible, the interaction remains a
  browser-based form submission.
test_inputs:
  postcode: PE22 0YD
