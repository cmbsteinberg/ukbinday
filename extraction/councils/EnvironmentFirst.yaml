council: EnvironmentFirst
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://environmentfirst.co.uk/house.php
api_methods:
- GET
api_description: The website provides a direct URL structure where the UPRN is passed
  as a query parameter to return bin collection data.
response_format: html
bin_selector: div.collect
date_format: '%d %B %Y'
test_inputs:
  uprn: '100060055444'
