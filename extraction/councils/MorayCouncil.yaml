council: MorayCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://bindayfinder.moray.gov.uk/disp_bins.php
api_methods:
- GET
api_description: The site takes an 8-digit zero-padded UPRN as a query parameter 'id'
  to display the current bin schedule.
response_format: html
bin_selector: div.container_images
date_format: '%d %B %Y'
test_inputs:
  uprn: '45438'
