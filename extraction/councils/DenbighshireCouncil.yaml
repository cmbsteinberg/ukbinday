council: DenbighshireCouncil
request_type: token_then_api
required_user_input:
- uprn
api_urls:
- https://refusecalendarapi.denbighshire.gov.uk/Csrf/token
- https://refusecalendarapi.denbighshire.gov.uk/Calendar/{user_uprn}
api_methods:
- GET
- GET
api_description: First call fetches a CSRF token. The second call uses the token in
  the 'x-csrf-token' header and the UPRN in the URL path to retrieve bin collection
  dates.
response_format: json
date_format: '%d/%m/%Y'
test_inputs:
  uprn: '200004299351'
