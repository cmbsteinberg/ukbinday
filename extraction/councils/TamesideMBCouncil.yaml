council: TamesideMBCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- http://lite.tameside.gov.uk/BinCollections/CollectionService.svc/GetBinCollection
api_methods:
- POST
api_description: A POST request is sent to the council's API service with the UPRN
  and specific parameters to retrieve bin collection dates in JSON format.
response_format: json
bin_selector: GetBinCollectionResult.Data
date_format: '%d/%m/%Y %H:%M:%S'
test_inputs:
  uprn: '100012835362'
