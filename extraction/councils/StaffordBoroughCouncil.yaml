council: StaffordBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.staffordbc.gov.uk/my-property-and-area-details
api_methods:
- GET
api_description: The council website uses a UPRN passed as a query parameter (uprn)
  to a property details page which contains a table with the id 'my-area' containing
  collection information.
response_format: html
bin_selector: table.my-area tr
date_format: Day DD Mon YYYY
test_inputs:
  uprn: '100032203010'
