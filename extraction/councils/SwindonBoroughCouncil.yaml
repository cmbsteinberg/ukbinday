council: SwindonBoroughCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.swindon.gov.uk/info/20122/rubbish_and_recycling_collection_days
api_methods:
- GET
api_description: A GET request is made directly to the collection days page with the
  addressList (uprn) and uprnSubmit parameters in the query string.
response_format: html
bin_selector: div.bin-collection-content
date_format: '%A, %d %B %Y'
test_inputs:
  uprn: '10022793351'
