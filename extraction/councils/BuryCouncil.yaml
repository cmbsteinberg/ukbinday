council: BuryCouncil
request_type: id_lookup_then_api
required_user_input:
- house_number
- postcode
api_urls:
- https://www.bury.gov.uk/app-services/getProperties?postcode={postcode}
- https://www.bury.gov.uk/app-services/getPropertyById?id={id}
api_methods:
- GET
- GET
api_description: First, the postcode is used to retrieve a list of properties and
  their IDs. Then, the specific property ID is used to fetch the bin collection schedule.
response_format: json
date_format: '%A %d %B %Y'
test_inputs:
  postcode: M26 3XY
