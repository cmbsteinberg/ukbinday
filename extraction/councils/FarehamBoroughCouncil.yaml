council: FarehamBoroughCouncil
request_type: single_api
required_user_input:
- postcode
api_urls:
- https://www.fareham.gov.uk/internetlookups/search_data.aspx
api_methods:
- GET
api_description: The API takes a postcode and returns a JSON object containing bin
  collection information in a string format within a 'rows' array.
response_format: json
date_format: '%d/%m/%Y'
test_inputs:
  postcode: PO14 4NR
