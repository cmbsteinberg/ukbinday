council: SouthLanarkshireCouncil
request_type: single_api
required_user_input:
- postcode
- address_label
api_urls:
- https://www.southlanarkshire.gov.uk/directory/145/bin_collection_days
api_methods:
- POST
api_description: '[AUTO-CORRECTED from id_lookup_then_api] The site uses a standard
  multi-step form. First, a POST request is made with the postcode to retrieve a list
  of addresses. Then, a second POST request is made with the selected address ID or
  label to retrieve the specific collection calendar page.'
api_headers:
  Content-Type: application/x-www-form-urlencoded
  User-Agent: Mozilla/5.0
api_payload_example: postcode=ML11+0FD&submit=submit
response_format: html
bin_selector: .bin-dir-snip
date_format: '%A %d %B %Y'
