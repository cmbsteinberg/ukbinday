council: DundeeCityCouncil
request_type: single_api
required_user_input:
- uprn
api_urls:
- https://www.dundee-mybins.co.uk/get_calendar.php?rn={uprn}
api_methods:
- GET
api_description: A direct GET request to the council's bin calendar endpoint using
  the UPRN as a query parameter. The response is a JSON array of collection events.
response_format: json
date_format: '%Y-%m-%d'
test_inputs:
  uprn: '9059043390'
